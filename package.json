{
  "name": "bubudati",
  "private": true,
  "scripts": {
    "build": "node scripts/build.js",
    "dev": "node scripts/build.js && npx --yes serve public -l 3000"
  },
  "engines": {
    "node": ">=18"
  }
}
